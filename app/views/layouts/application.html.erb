<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Scipod" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <script src="https://kit.fontawesome.com/3726576b54.js" crossorigin="anonymous"></script>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <!-- Original Navigation Header (Commented out - replaced with government-style search header) -->
    <%# TODO: Remove this commented section after confirming new header works properly %>
    <!--
    <header class="main-header">
      <nav class="nav-container">
        <%= link_to "SciPod", root_path, class: "logo" %>

        <div class="user-nav">
          <% if user_signed_in? %>
            <%= link_to "Create Podcast", new_podcast_path, class: "btn-primary" %>
            <div class="user-menu">
              <span class="user-email"><%= current_user.email %></span>
              <div class="user-dropdown">
                <%= link_to "My Podcasts", my_podcasts_user_path(current_user) %>
                <%= link_to "My Favorites", favorites_user_path(current_user) %>
                <%= link_to "Profile", user_path(current_user) %>
                <%= link_to "Logout", destroy_user_session_path, data: { "turbo-method": :delete } %>
              </div>
            </div>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "btn-secondary" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "btn-primary" %>
          <% end %>
        </div>
      </nav>
    </header>
    -->

    <%= render 'shared/search_header' %>

    <%= render 'shared/flash_messages' %>

    <!-- Main Content -->
    <main class="main-content" data-controller="page-transition">
      <%= yield %>
    </main>
  </body>
</html>
